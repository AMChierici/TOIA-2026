# Some dependencies (e.g. @brainhubeu/react-carousel) declare a React 16 peer
# range while the app runs on React 17. npm >= 7 enforces peer deps strictly and
# would refuse to install, so allow the legacy (npm 6) resolution behaviour.
legacy-peer-deps=true
