# Frontend build configuration.
# Copy to `interface/.env`. Only needed for local (non-docker) `npm start`.

# Avoids a Create React App / babel preflight version check failing the build.
SKIP_PREFLIGHT_CHECK=true
